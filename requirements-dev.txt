# Only the Docker image builds still read this file. uv manages the host
# environment from pyproject.toml and uv.lock. Keep this file in sync with
# the `dev` dependency group until the image builds move to uv as well.
prek~=0.4.13
# The type gate runs this exact release, so a new pyright cannot turn CI red
# on its own. Raise it deliberately.
pyright==1.1.411
pytest-cov~=7.1
pytest-xdist~=3.5
pytest-sugar~=1.1
hypothesis~=6.163
depthai>=3.8.0,<4
# Lets the tests emit tiny .tflite fixtures directly (no TensorFlow needed).
flatbuffers~=25.12
tflite2onnx~=0.1
