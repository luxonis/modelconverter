# Only the Docker image builds still read this file. uv manages the host
# environment from pyproject.toml and uv.lock. Keep this file in sync with
# [project.dependencies] until the image builds move to uv as well.
Pillow
luxonis-ml[data,nn_archive,s3,gcs,telemetry]~=0.9.0
cyclopts~=4.16
onnx==1.21.0
onnxruntime
onnxsim~=0.6
onnxslim~=0.1
onnxsim
docker
keyring
hubai-sdk>=0.3.3
onnx_graphsurgeon
wget
aiobotocore<2.18
psutil
